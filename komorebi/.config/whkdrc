.shell powershell

# Reload whkd configuration
# alt + o                 : taskkill /f /im whkd.exe && start /b whkd # if shell is cmd
alt + o                 : taskkill /f /im whkd.exe; Start-Process whkd -WindowStyle hidden # if shell is pwsh / powershell
alt + shift + o         : komorebic reload-configuration

alt + i                 : komorebic toggle-shortcuts

# App shortcuts - these require shell to be pwsh / powershell
# The apps will be focused if open, or launched if not open
# alt + f                 : if ($wshell.AppActivate('Firefox') -eq $False) { start firefox }
# alt + b                 : if ($wshell.AppActivate('Chrome') -eq $False) { start chrome }

alt + q                 : komorebic close
alt + m                 : komorebic minimize

# Focus windows
alt + shift + h                 : komorebic focus left
alt + shift + j                 : komorebic focus down
alt + shift + k                 : komorebic focus up
alt + shift + l                 : komorebic focus right
alt + shift + oem_4     : komorebic cycle-focus previous # oem_4 is [
alt + shift + oem_6     : komorebic cycle-focus next # oem_6 is ]

# Move windows
alt + shift + return    : komorebic promote
win + shift + h                 : komorebic move left
win + shift + j                 : komorebic move down
win + shift + k                 : komorebic move up
win + shift + l                 : komorebic move right

# Stack windows
alt + left              : komorebic stack left
alt + down              : komorebic stack down
alt + up                : komorebic stack up
alt + right             : komorebic stack right
alt + oem_4             : komorebic cycle-stack previous # oem_4 is [
alt + oem_6             : komorebic cycle-stack next # oem_6 is ]

# Resize
alt + shift + u : komorebic resize-axis horizontal increase
alt + shift + p : komorebic resize-axis horizontal decrease

# Manipulate windows
alt + t                 : komorebic toggle-float
alt + shift + f         : komorebic toggle-monocle
alt + s : komorebic cycle-workspace next
alt + a : komorebic cycle-workspace previous

# Window manager options
alt + shift + r         : komorebic retile
alt + p                 : komorebic toggle-pause

# Layouts
alt + x                 : komorebic flip-layout horizontal
alt + y                 : komorebic flip-layout vertical

# Workspaces
alt + shift + 1                 : komorebic focus-workspace 0
alt + shift + 2                 : komorebic focus-workspace 1
alt + shift + 3                 : komorebic focus-workspace 2
alt + shift + 4                 : komorebic focus-workspace 3
alt + shift + 5                 : komorebic focus-workspace 4
alt + shift + 6                 : komorebic focus-workspace 5
alt + shift + 7                 : komorebic focus-workspace 6
alt + shift + 8                 : komorebic focus-workspace 7

# Move windows across workspaces
win + shift + 1         : komorebic move-to-workspace 0
win + shift + 2         : komorebic move-to-workspace 1
win + shift + 3         : komorebic move-to-workspace 2
win + shift + 4         : komorebic move-to-workspace 3
win + shift + 5         : komorebic move-to-workspace 4
win + shift + 6         : komorebic move-to-workspace 5
win + shift + 7         : komorebic move-to-workspace 6
win + shift + 8         : komorebic move-to-workspace 7
