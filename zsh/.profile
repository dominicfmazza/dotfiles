# User specific environment and startup programs

export PATH=$PATH:$HOME/.local/bin:$HOME/bin

. "/disk01/users/dom/.local/share//../bin/env"

. "$HOME/.local/share//../bin/env"
. "$HOME/.cargo/env"

export PATH="$HOME/.local/share/mise/shims:$PATH"
